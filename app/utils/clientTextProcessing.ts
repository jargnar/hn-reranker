'use client';

import { preprocessText, calculateSimilarity, extractKeywords } from './textProcessing';

// Re-export the functions for client-side use
export { preprocessText, calculateSimilarity, extractKeywords }; 